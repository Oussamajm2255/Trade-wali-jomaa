# Secrets — never commit
.env

# Local databases
*.db
*.sqlite3

# Held-out validation cache (fetched once, never retouched)
_heldout_cache/

# Private / generated artifacts
conversation.txt
report.txt

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.venv/
venv/
build/
dist/
*.egg-info/
